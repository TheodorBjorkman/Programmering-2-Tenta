using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Body
{
    public class Movement : Body
    {
        void Update()
        {
            /*
            xPos = transfor
            yPos =
            xMove = Math.Cos(angle) * this.moveSpeed;
            xMove = Math.Sin(angle) * this.moveSpeed;
            this.xPos += xMove;
            this.yPos += yMove;
            */
        }
    }

}
